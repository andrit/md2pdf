[package]
name = "typst_embed_spike"
version = "0.1.0"
edition = "2021"

# Pinned exactly: the typst crate API is explicitly unstable across releases.
[dependencies]
typst = "=0.15.1"
typst-layout = "=0.15.1"
typst-pdf = "=0.15.1"
typst-render = "=0.15.1"
typst-kit = { version = "=0.15.1", features = ["embedded-fonts"] }
serde_json = "1"
comemo = "0.5"
