//! Platform config directory via `directories`, keyed off the `BundleId`.
//!
//! The reverse-DNS bundle id is set from day one because macOS derives the config
//! and data directories from it — changing it later strands user settings.
//!
//! Not yet implemented.
