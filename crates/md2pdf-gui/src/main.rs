//! The egui shell. Draws read models; decides nothing.
//!
//! Written after `cargo vendor` made `eframe` readable here — see `design/plan-app.md`.
fn main() {
    todo!("phase 4 GUI — written once eframe is vendored");
}
